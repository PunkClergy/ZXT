<view class="container" style="background-image: url('{{s_background_picture_of_the_front_page}}');height: {{c_screen_height}}px;">
  <custom-header title="管理层角色" />
  <!-- index.wxml -->
  <view class="record-container" style="height: {{c_screen_height-(navBarHeight + statusBarHeight)}}px;margin-top: {{navBarHeight + statusBarHeight}}px;">
    <!-- 切换部分 -->
    <view class="record-tabs" bindtap="handleSwitchTab">
      <view class="record-tabs-item" style="background-image: url('{{c_activeTab == 1?s_background_tabs_active_1:s_background_tabs_1}}');">人员列表</view>
      <view class="record-tabs-item" style="background-image: url('{{c_activeTab == 2?s_background_tabs_2:s_background_tabs_active_2}}');">权限管理</view>
    </view>
    <!-- 详情区域部分 -->
    <block wx:if="{{c_activeTab == 1}}">
      <scroll-view scroll-y style="height:{{c_screen_height-(navBarHeight + statusBarHeight + 10 + 50)}}px;" bindscrolltolower="handleLower" bindrefresherrefresh="handleRefresh" refresher-enabled="true" refresher-triggered="{{g_triggered}}">
        <block wx:for="{{g_items}}" wx:key="index">
          <view class="content-item">
            <view class="content-item-head">
              <view class="head-left">
                <view class="left-category">
                  <text>{{item.name}}</text>
                </view>
              </view>
              <view class="head-right">
                <image src="/assets/images/home/_edit.png" data-item="{{item}}" bindtap="handleEdit" />
                <image src="/assets/images/home/_delete.png" data-id="{{item.id}}" bindtap="handleDelete" />
              </view>
            </view>
            <view class="content-item-info">
              <view class="info-item  {{item.bak.length > 15 ? 'long-info-item' : ''}}">
                <label>说明 ：</label>
                <text>{{item.bak||'-'}}</text>
              </view>
            </view>
          </view>
        </block>
      </scroll-view>
    </block>
    <block wx:if="{{c_activeTab == 2}}">
      <scroll-view scroll-y style="height:{{c_screen_height-(navBarHeight + statusBarHeight + 10 + 50)}}px;">
        <view class="long-range-control-car">
          <view style="height: 30rpx;"></view>
          <block>
            <!-- <view class="card-head">
              <text>权限设置</text>
            </view> -->
            <view class="long-range-control-car-subitem">
              <view class="tree-container">
                <block wx:for="{{tree}}" wx:key="id">
                  <view class="node">
                    <!-- 一级节点 -->
                    <view class="node-header">
                      <view class="arrow {{item.isExpanded ? 'expanded' : ''}}" wx:if="{{item.children.length}}" bindtap="toggleExpand" data-id="{{item.id}}">▶</view>
                      <view class="checkbox {{item.checked ? 'checked' : ''}} {{item.indeterminate ? 'indeterminate' : ''}}" data-id="{{item.id}}" bindtap="handleCheck">
                        <view class="checkmark" wx:if="{{item.checked}}">✓</view>
                        <view class="indeterminate-line" wx:if="{{item.indeterminate}}"></view>
                      </view>
                      <text class="node-name">{{item.name}}</text>
                    </view>

                    <!-- 二级节点 -->
                    <view class="children" wx:if="{{item.children.length && item.isExpanded}}">
                      <block wx:for="{{item.children}}" wx:key="id">
                        <view class="child-node">
                          <view class="node-header">
                            <view class="arrow {{item.isExpanded ? 'expanded' : ''}}" wx:if="{{item.children.length}}" bindtap="toggleExpand" data-id="{{item.id}}">▶</view>
                            <view class="checkbox {{item.checked ? 'checked' : ''}} {{item.indeterminate ? 'indeterminate' : ''}}" data-id="{{item.id}}" bindtap="handleCheck">
                              <view class="checkmark" wx:if="{{item.checked}}">✓</view>
                              <view class="indeterminate-line" wx:if="{{item.indeterminate}}"></view>
                            </view>
                            <text class="node-name">{{item.name}}</text>
                          </view>

                          <!-- 三级节点 -->
                          <view class="children" wx:if="{{item.children.length && item.isExpanded}}">
                            <block wx:for="{{item.children}}" wx:key="id">
                              <view class="child-node">
                                <view class="node-header">
                                  <view class="arrow {{item.isExpanded ? 'expanded' : ''}}" wx:if="{{item.children.length}}" bindtap="toggleExpand" data-id="{{item.id}}">▶</view>
                                  <view class="checkbox {{item.checked ? 'checked' : ''}} {{item.indeterminate ? 'indeterminate' : ''}}" data-id="{{item.id}}" bindtap="handleCheck">
                                    <view class="checkmark" wx:if="{{item.checked}}">✓</view>
                                    <view class="indeterminate-line" wx:if="{{item.indeterminate}}"></view>
                                  </view>
                                  <text class="node-name">{{item.name}}</text>
                                </view>

                                <!-- 四级节点 -->
                                <view class="children" wx:if="{{item.children.length && item.isExpanded}}">
                                  <block wx:for="{{item.children}}" wx:key="id">
                                    <view class="child-node">
                                      <view class="node-header">
                                        <view class="arrow {{item.isExpanded ? 'expanded' : ''}}" wx:if="{{item.children.length}}" bindtap="toggleExpand" data-id="{{item.id}}">▶</view>
                                        <view class="checkbox {{item.checked ? 'checked' : ''}} {{item.indeterminate ? 'indeterminate' : ''}}" data-id="{{item.id}}" bindtap="handleCheck">
                                          <view class="checkmark" wx:if="{{item.checked}}">✓</view>
                                          <view class="indeterminate-line" wx:if="{{item.indeterminate}}"></view>
                                        </view>
                                        <text class="node-name">{{item.name}}</text>
                                      </view>

                                      <!-- 五级节点 -->
                                      <view class="children" wx:if="{{item.children.length && item.isExpanded}}">
                                        <block wx:for="{{item.children}}" wx:key="id">
                                          <view class="child-node">
                                            <view class="node-header">
                                              <view class="checkbox {{item.checked ? 'checked' : ''}}" data-id="{{item.id}}" bindtap="handleCheck">
                                                <view class="checkmark" wx:if="{{item.checked}}">✓</view>
                                              </view>
                                              <text class="node-name">{{item.name}}</text>
                                            </view>
                                          </view>
                                        </block>
                                      </view>
                                    </view>
                                  </block>
                                </view>
                              </view>
                            </block>
                          </view>
                        </view>
                      </block>
                    </view>
                  </view>
                </block>
              </view>

            </view>
          </block>
          <view class="card-footer">
            <view bindtap="handleSubmit">确认{{btnState}}</view>
          </view>
        </view>

      </scroll-view>
    </block>
    <!-- 一键操作按钮 -->
    <view class="levitation-button" bindtap="handleJumpInfo">
      <text>新增人员</text>
    </view>
  </view>

</view>

<!-- 新增人员 -->
<block>
  <view class="modal-mask" wx:if="{{c_send_key_show_momal}}" bindtap="handleHideSengKeyModal"></view>
  <view class="modal-base-map" wx:if="{{c_send_key_show_momal}}">
    <form bindsubmit="handleFormSubmit">
      <view class="modal-container">
        <view class="modal-container-head">
          <text>新增人员</text>
          <image src="/assets/images/home/right_1.png" bindtap="handleHideSengKeyModal" />
        </view>
        <view class="modal-container-middle">
          <view class="middle-form-item">
            <label>账号</label>
            <view class="modal-form-region">
              {{cellData.platenumber}}
            </view>

          </view>
          <view class="middle-form-item">
            <label>密码</label>
            <view class="modal-form-region">
              <input placeholder="请输入使用人" name='personName' style="text-align: right;font-size: 28rpx;" />
            </view>
          </view>
          <view class="middle-form-item">
            <label>姓名</label>
            <view class="modal-form-region">
              <input placeholder="请输入手机号" name='mobile' style="text-align: right;font-size: 28rpx;" />
            </view>

          </view>
          <view class="middle-form-item">
            <label>手机号</label>
            <view class="modal-form-region">
              <picker mode="date" data-index="startDate" bindchange="bindTimeChange">
                <view class="form-item-text">
                  <text>{{startDate}}</text>
                </view>
              </picker>
              <picker mode="time" data-index="startTime" bindchange="bindTimeChange">
                <view class="form-item-text">
                  <text>{{startTime}}</text>
                </view>
              </picker>
            </view>

          </view>
        </view>
        <view class="modal-container-footer">
          <button formType="submit">确认</button>
        </view>
      </view>
    </form>
  </view>
</block>