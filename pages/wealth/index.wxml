<view class="container" style="background-image: url('{{s_background_picture_of_the_front_page}}');height: {{c_screen_height}}px;">
  <custom-header title="财务保管原车钥匙" />
  <!-- index.wxml -->
  <view class="record-container" style="height: {{c_screen_height-(navBarHeight + statusBarHeight)}}px;margin-top: {{navBarHeight + statusBarHeight}}px;">
    <!-- 切换部分 -->
    <view class="record-tabs" bindtap="handleSwitchTab">
      <view class="record-tabs-item" style="background-image: url('{{c_activeTab == 1?s_background_tabs_active_1:s_background_tabs_1}}');">保管钥匙列表</view>
      <view class="record-tabs-item" style="background-image: url('{{c_activeTab == 2?s_background_tabs_2:s_background_tabs_active_2}}');">{{btnState}}钥匙保管</view>
    </view>
    <!-- 详情区域部分 -->
    <block wx:if="{{c_activeTab == 1}}">
      <scroll-view scroll-y style="height:{{c_screen_height-(navBarHeight + statusBarHeight + 10 + 50)}}px;" bindscrolltolower="handleLower" bindrefresherrefresh="handleRefresh" refresher-enabled="true" refresher-triggered="{{g_triggered}}">
        <block wx:for="{{g_items}}" wx:key="index">
          <view class="content-item">
            <view class="content-item-head">
              <view class="head-left">
                <view class="left-category">
                  <image src="/assets/images/home/car_icon.png" />
                  <text>{{item.platenumber}}</text>
                </view>
              </view>
              <view class="head-right" wx:if="{{!(g_flagMulti)}}">
                <image src="/assets/images/home/_edit.png" data-item="{{item}}" bindtap="handleEdit" />
              </view>
            </view>
            <view class="content-item-info">
              <view class="info-item  {{item.count.length > 15 ? 'long-info-item' : ''}}">
                <label>原车钥匙数量 ：</label>
                <text>{{item.count||0}}</text>
              </view>
              <view class="info-item  {{item.type.length > 8 ? 'long-info-item' : ''}}">
                <label>是否保管 ：</label>
                <text>{{item.type == 1?'是':'否'}}</text>
              </view>
              <view class="info-item  {{item.createdate.length > 8 ? 'long-info-item' : ''}}">
                <label>保管时间 ：</label>
                <text>{{item.createdate}}</text>
              </view>

            </view>
          </view>
        </block>

      </scroll-view>
    </block>
    <block wx:if="{{c_activeTab == 2}}">
      <scroll-view scroll-y style="height:{{c_screen_height-(navBarHeight + statusBarHeight + 10 + 50)}}px;">

        <view class="card-info">
          <!-- 车牌号 -->
          <view class="card-info-item">
            <label>车牌号<text>*</text></label>
            <block>
              <view class="card-info-item-input">
                <input placeholder="请输入车牌号" value="{{params.platenumber}}" data-item="platenumber" bindinput="handleBindinput" />
              </view>
            </block>

          </view>
       
          <!-- 原车钥匙数量 -->
          <view class="card-info-item">
            <label>原车钥匙数量</label>

            <block>
              <view class="card-info-item-input">
                <input placeholder="请输入原车钥匙数量" value="{{params.count}}" data-item="count" bindinput="handleBindinput" />
              </view>
            </block>
          </view>
          <!-- 是否保管 -->
          <view class="card-info-item">
            <label>是否保管</label>
            <block>
              <view class="card-info-item-tabs">
                <view class="card-info-item-tabs-btn">
                  <text class="{{batterylift == 1?'tabs-active':''}}" data-item="1" bindtap="handleBatterylift">是</text>
                  <text class="{{batterylift == 2?'tabs-active':''}}" data-item="2" bindtap="handleBatterylift">否</text>
                </view>
              </view>
            </block>
          </view>
        </view>
        <view class="card-footer">
          <view bindtap="handleSubmit">确认{{btnState}}</view>
        </view>
      </scroll-view>
    </block>
  </view>
</view>